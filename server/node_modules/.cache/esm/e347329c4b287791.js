let express,readdirSync,cors,mongoose;_4b2‍.w("express",[["default",["express"],function(v){express=v}]]);_4b2‍.w("fs",[["readdirSync",["readdirSync"],function(v){readdirSync=v}]]);_4b2‍.w("cors",[["default",["cors"],function(v){cors=v}]]);_4b2‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);



// const morgan = require("morgan");

dotenv.config();

 const app = express();

// db connection
mongoose
  .connect(process.env.DATABASE, {
    useNewUrlParser: true,
    useFindAndModify: false,
    useUnifiedTopology: true,
    useCreateIndex: true,
  })
  .then(() => console.log("DB Connected"))
  .catch((err) => _4b2‍.g.console.log("DB Connection Error: ", err));

// middlewares
app.use(cors());
app.use(morgan("dev"));

// route middleware
readdirSync("./routes").map((r) => app.use("/api", require(`./routes/${r}`)));

const port = process.env.PORT || 8000;

app.listen(port, () => console.log(`Server is running on port ${port}`));
